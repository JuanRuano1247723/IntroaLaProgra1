using System;

namespace ValoresVariablesConstantes
{
    class Program
    {
        static void Main(string[] args)
        {
            Double pi = Math.PI;
            bool r = false;

            Console.WriteLine(Math.Round(pi,4));
            Console.WriteLine(r);

            Console.ReadKey();
        }
    }
}
